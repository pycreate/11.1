# #2.c语言基础

## 逻辑运算符与判等运算符

判等运算符：==和！=

例如：

5==5是1；

5!=5是0；

逻辑运算符：与（&&）或（||）非（！）

<span style="font-weight:bold;">运算逻辑与数电中逻辑门一致</span>

```c
#include<stdio.h>//
int a=2;
int b=3;
int c=5;


int main()
{
    a=(b>c) && (c==5);
    printf("a=%d",a);
}
/*运行结果：因为b>c（为假0）&&（与）c==5（为真1）
a=0与1；a的结果为0 */
```

## 选择判断语句

```c
#include<stdio.h>
int a;
int b;
int main()
{
    printf("请输入一个整数：");
    scanf("%d",&a);
    if(a%2==0)
        printf("这是一个偶数");
    else
    {
        printf("这是一个奇数,我将把b输出");
    	b=a*a;
    	printf("\nb=%d",b);
    }
    
    
}
```

if....else语句

if（）后添加进行判断的条件

### if...else嵌套

```c
#include<stdio.h>//if...else嵌套（判断成绩等级）
float num;
int b;
int main()
{
    printf("请输入你的成绩:");
    scanf("%f",&num);
    if(num<60)
    {
       printf("你的成绩不合格,等级：D");
    }
    else if(num>=60 && num<75)
    {
       printf("你的成绩等级是C");
    }
    else if(num>=75 && num<85)
    {
        printf("你的成绩等级是B");
    }
    else if(num>=85 && num<=100)
    {
        printf("你的成绩等级是A");
    }
    
}
```

if可以独立出现，但是else归属于最近的if

### if单独出现的情况

```c
#include<stdio.h>
int a;
int b,c=1;
int main()
{
    printf("输入a:");
    scanf("%d",&a;);
    printf("\n输入b:");
    scanf("%d",&b);
    if(a==0)
    {
        printf("c=%d",c);
    }
    if(a>=b)
    {
        a=a+b+c;
        printf("a=%d",a);
    }
    else
    {
        a=0;
        printf("a=%d输入错误",a);
    }
}
```

```c
#include<stdio.h>
int a;
int b,c;
int main()
{
    printf("a=");
    scanf("%d",&a);
    printf("b=");
    scanf("%d",&b);
    c=b>a?b:a;//b>a为真，则将b赋值给a，若为假，则将a赋值给c
    printf("c=%d",c);
}
```

## Bool类型

Bool表示逻辑关系，只有0或者1

例如：

```c
#include<stdio.h>
int a;
_Bool b=10;
//bool数据类型只有当赋值为0的时候输出为0，其余任何赋值，输出结构都为1
int main()
{
    printf("b=%d",b);
}
```

关于if的：

```c
#include<stdio.h>
int a;

int main()
{
    printf("a=");//同上bool类型
    scanf("%d",&a);
    if(a)
    {
        printf("这是真的");
    }
    else
    {
        printf("这是假的");
    }
}
```

当a=0时，if判断后输出的是”“这是假的”，当a等于除0以外的任何数时，输出”“这是真的”

## 选择语句switch

switch语句执行逻辑：找到与之匹配的常量，开始顺序往后执行

```c
#include<stdio.h>//switch语句
int num;
int b;
int main()
{
    printf("请输入你的成绩:");
    scanf("%d",&num);
    
    switch(num)
    {
        case 0:
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
           printf("你的成绩不合格,等级：D");
            break;
        case 6:
           printf("你的成绩等级是C");
             break;
        case 7:
           printf("你的成绩等级是B");
             break;
        case 8:
        case 9:
        case 10:
           printf("你的成绩等级是A");
             break;
        default:
            printf("成绩输入有错误");
    }
}
```

switch语句下属选项case（）后跟具体的表达式或数值（不能跟不等式等）；后按顺序执行，在每一段case后跟一句break（跳出switch语句，直接输出最终结果）



















