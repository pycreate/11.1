# #1.c语言基础

## 转移序列（换行）

```c
#include<stdio.h>

int main()
{
    printf("hello world");
    printf("\nhello world \n hello k");
}
/*输出结果：
hello world
hello world
hello k*/

```

## 占位符，输入输出

```c
#include<stdio.h>
int a;//定义变量a
int b;//定义变量b

int main()
{
    printf("请输入一个数");
    scanf("%d",&a);
    b = a*a;
    printf("b=%d",b);        
}
```

定义变量：常用int型（整形）、float型（浮点型）等

整形（int）的占位符：%d；

%n.md(n为小数点前的数字精度，m为小数点后的数字精度)

浮点型（float）的占位符：%f；

%n.mf(n为小数点前的数字精度，m为小数点后的数字精度)

宏定义：#define

例子：计算圆的体积

```c
#include<stdio.h>
#define PI 3.14f//用宏定义将PI定义为3.14
int r;//圆的半径
int high;//圆的高
float v;//圆的体积
float s;//圆的面积

int main()
{
    printf("输入圆的半径:");
    scanf("%d",&r);
    printf("输入圆柱的高:");
    scanf("%d",&high);
    s= PI*r*r;
    v= s*high;
    printf("圆的体积是：%2.5f",v);
       
}
```

## c语言的计算、逻辑判断

```c
#include<stdio.h>
int a=3;//定义变量a
int b=4;//定义变量b
int c=5,d=1;//同上
int main()
{
    a=b-3;
    printf("a=%d\nb=%d",a,b);
    c+=5;//等价c=c+5
    printf("\nc=%d",c);
    d=b++;//运行过程：先b=d，后b++
    printf("\nd=%d\nb=%d",d,b);
    d=++b;//运行过程：先b++，后将b++的结果等于d
    printf("\nd=%d\nb=%d",d,b);
} 
/*输出结果：
a=1
b=4
c=10
d=4
b=5
d=6
b=6
*/
```

上述程序中：a++（自加），a--（自减）

赋值：是将等号右边的赋值给左边的，例如：a=3（将3赋值给a）

```c
#include<stdio.h>
int a;
int main()
{
    printf("请输入一个数");
    scanf("%d",&a);
    a=a*a;
    printf("a=%d",a);
    
}
//其中a=a*a是将a的平方重新赋值给a

```

## 逻辑判断语句和条件语句

### 判断语句

```c
#include<stdio.h>
int a,b;
int c,d;

int main()
{
    a=2;
    b=3;
    c=a>b;
    d=a<b;
    printf("c=%d",c);
    printf("d=%d",d);
    
}
/*输出结果：
c=0（表示判断a<b这个是错误的，输出是0，将这个结果赋值给c）
d=1（表示判断a<b这个是正确的，输出是1，将这个结果赋值给d）
```

### 条件语句

```c
#include<stdio.h>
int a=3;
int b=4;
int c=5,d=1;
int number;
int main()
{
	printf("请输入一个数字:");
	scanf("%d",&number);
	if(number%2==0)
	{
		number =number*5;
		printf("这是一个偶数%d",number);
	}
	else
	{
		number = number*3; 
		printf("这是一个奇数%d",number);
	}
	
}
/*判断:输入一个数,这个数是奇数还是偶数,如果是奇数,将原数字乘3后输出;如果是偶数,将原数字乘5后输出
```

以上程序中存在多功能嵌套,将条件判断后输出数字和输出数字计算结合,用中括号将两个功能结合输出

























